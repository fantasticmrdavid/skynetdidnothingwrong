import styled from 'styled-components';

export const Logo = styled.div`
  position: relative;
  z-index: 1;
  text-align: center;
  font-size: 2rem;
  padding: 1em 0;
  text-transform: lowercase;
`;
